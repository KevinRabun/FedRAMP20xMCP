# Evaluation Reports

This directory contains generated evaluation reports.

Reports are generated in JSON, Markdown, or HTML format and include:
- Overall score and pass rate
- Category-level metrics
- Individual test case results
- Failure details

Reports are not committed to the repository (except this README).
